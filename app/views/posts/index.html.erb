<!-- Background wrapper -->
<div class="bg-themed">

  <!-- Header: Title + Create Button -->
  <div class="container">
    <div class="posts-header">
      <h1>All Posts</h1>
      <%= button_to "➕ New Post", new_post_path, method: :get, class: "btn btn-primary shadow-sm" %>
    </div>

    <!-- Posts list -->
    <% if @all_posts.any? %>
      <div class="row g-4 posts-list">
        <%= render partial: "posts/show", collection: @all_posts, as: :post %>
      </div>
    <% else %>
      <div class="alert alert-info text-center">
        No posts found. Be the first to create one!
      </div>
    <% end %>
  </div>
  
</div>
