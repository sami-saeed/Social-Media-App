<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Social Media" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= favicon_link_tag asset_path('favicon-32x32.png') , type: 'image/png'%>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <nav class="navbar-custom">
      <div class="container d-flex justify-content-between align-items-center">
        <div class="navbar-brand d-flex align-items-center">
          <%= image_tag 'favicon-32x32.png', alt: "Postoria Logo", width: 24, height: 24 %>
          <a class="navbar-brand fs-4 fw-bold ms-1" href="/">Postoria</a>
        </div>
        <div class="d-flex align-items-center">
          <% if user_signed_in? %>
           
            <div data-controller="notifications" class="position-relative me-3">
              <a href="#"
                 data-action="click->notifications#toggle"
                 data-notifications-target="bell"
                 class="nav-link text-light position-relative"
                 role="button"
                 aria-haspopup="true"
                 aria-expanded="false">
                🔔
                <span id="unread_count"
                      class="badge bg-danger position-absolute top-0 start-100 translate-middle rounded-pill">
                  <%= current_user.notifications.unread.count %>
                </span>
              </a>

              <div data-notifications-target="list"
                   id="notifications_list"
                   class="notifications-list"
                   style="display: none; position: absolute; right: 0; background: white; border: 1px solid #ccc; width: 320px; max-height: 300px; overflow-y: auto; z-index: 1000;">
                <ul id="notifications_menu" class="list-unstyled mb-0 p-2">
                  <% current_user.notifications.order(created_at: :desc).limit(5).each do |notification| %>
                    <li class="notification-item px-2 py-1 border-bottom">
                      <%= notification.action %> — <%= time_ago_in_words(notification.created_at) %> ago
                    </li>
                  <% end %>
                </ul>
                <div class="d-flex justify-content-center align-items-center p-2">
                  <%= button_to "View All", notifications_path, method: :get, class: "btn user-btn btn-sm mt-1 me-2" %>
                  <%= button_to "Mark All read", mark_all_as_read_notifications_path, method: :patch,
                                 form: { data: { turbo: true } },
                                 class: "btn user-btn btn-sm mt-1" %>
                </div>
              </div>
            </div>

            <!-- User Info -->
            <div class="me-3 d-flex align-items-center">
              <strong class="me-2 text-light">
                <%= link_to current_user.username, user_path(current_user), class: "link-success text-decoration-none" %>
              </strong>
              <% if current_user.avatar.attached? %>
                <%= image_tag url_for(current_user.avatar), class: "rounded-circle", width: 40, height: 40 %>
              <% end %>
            </div>

            <!-- Logout -->
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn user-btn text-dark btn-sm" %>

          <% else %>
            <%= button_to "Login", new_user_session_path, method: :get, class: "btn user-btn text-dark btn-sm me-2" %>
            <%= button_to "Sign Up", new_user_registration_path, method: :get, class: "btn user-btn text-dark btn-sm" %>
          <% end %>
        </div>
      </div>
    </nav>

    <% if notice %>
      <div class="alert alert-success text-center py-2 shadow-sm">
        <%= notice %>
      </div>
    <% end %>

    <% if alert %>
      <div class="alert alert-danger text-center py-2 shadow-sm">
        <%= alert %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
